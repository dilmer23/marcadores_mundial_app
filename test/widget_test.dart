import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App loads without errors', (WidgetTester tester) async {
    // App requires Supabase.initialize() which needs a real project.
    // This is a placeholder test for CI.
    expect(true, isTrue);
  });
}
